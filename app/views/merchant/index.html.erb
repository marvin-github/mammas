<div class="container">
  <h1>Merchants</h1>

  <div class="table-responsive">
    <table class="table table-striped">
      <thead>
      <tr>
        <th>Merchant</th>
        <th>Discount</th>
      </tr>
      </thead>
      <tbody>
      <% @merchant.each do |m| %>
          <tr>
            <td><%= m.merchant_name + ' ' + m.store_number %></td>
            <td><%= m.discount %></td>
            <td><%= link_to 'Edit', edit_merchant_path(m), :class => "btn btn-warning"  %></td>
          </tr>




      <% end %>
      </tbody>
    </table>
  </div>

</div>

<br>



